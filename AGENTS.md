# AGENTS.md

> 最后修改时间：2026-09-24 18:16 CST
> 文档版本：v3
> 修改说明：增加文件格式必须遵守 `.editorconfig` 和 `.clang-format` 的约束。

本文件定义工作区根目录的协作规范。子目录中的 `AGENTS.md` 与本文件同时生效；进入子目录后，以范围更具体的规范为准。文中的路径均相对于工作区根目录。

## 适用范围与协作方式

- 所有任务由当前主代理独立完成。不得创建、调用或向子代理委派工作。
- 所有修改必须位于当前工作区。除非任务明确要求，或不修改就无法完成，否则不得改动第三方项目、基线实现及其配置和文档。
- 不得以统一风格、顺手重构或预备兼容为由扩大修改范围。
- 未经用户明确指示，不得读取、参考或复用其他工作区的内容。

## 任务执行顺序

### 1. 确认基线

安装、构建或升级依赖前，先阅读项目已有的环境基线、构建指南和安装指南。若没有环境基线，先核实本次操作涉及的硬件、操作系统和工具链。

同时检查依赖的体系结构、语言运行时版本、ABI，以及相关驱动和加速库的兼容性。使用项目指定的隔离环境和依赖来源，不得用系统环境、全局环境或其他项目环境替代。

未经用户明确批准，不得升级或替换宿主操作系统、内核、驱动或系统级计算组件，也不得直接运行包含这些操作的上游安装脚本。依赖要求与环境基线冲突时，停止安装并报告冲突。

### 2. 明确实现边界

一项功能只有在完整覆盖另一项功能的需求和调用场景时，才可以作为唯一实现。此时删除被替代实现、兼容层、分支路径和重复维护逻辑，不为完全替代扩张职责或加入无关能力。覆盖不完整时，不得判定为完全替代。

研究可以探索多个方案。某个方案经实验记录确认无效后，删除其实现、配置开关、调用入口、专属测试和兼容逻辑；保留配置、日志、原始结果和分析作为淘汰依据。

### 3. 实现与验证

新建或修改文件时，必须遵守仓库根目录的 `.editorconfig`；`.clang-format` 支持的源码还必须遵守仓库根目录的 `.clang-format`。

执行研究任务时，使用匹配的专项 skill，并遵守其前置流程、任务边界、证据要求和产物约定。

研究实现完成后先做小规模验证，再根据实测结果决定保留、删除、修正或进入完整实验。耗时较长的完整实验默认不运行；只有小规模验证显示可信收益且需要最终确认，或关键结论仍无法判定时，才运行完整实验。

对比实验必须保持每套技术栈内部一致，分别使用各自的模型、运行时、动态库、适配器、编译配置和安装目录，不得交叉混用。对比各方应对齐影响结论的编译语义、运行配置和调优条件；实验变量、无法对齐的条件以及模型或产物是否调优、采用何种方法，都要如实记录。

每次实验都要核验并记录实际配置和产物来源，不得仅凭目录名推断构建选项或运行语义。

### 4. 构建、安装与发布

正式构建、临时验证构建和安装产物使用独立目录。临时验证不得覆盖正式构建或更新正式安装。

发布前完成正式构建和必要验证，再检查待安装产物，最后更新正式安装。正式安装目录只保存通过验证的产物，不得在其中编译或手工修改文件。

未经明确授权，不得把项目依赖安装到系统目录或全局运行环境。改变构建选项、目录或安装流程时，须在同一任务中同步更新操作指南。

## 命令输出与网络访问

- 网络访问遵循当前环境的连接和代理配置，不得套用其他环境的地址或端口。
- 命令的标准输出和标准错误必须直接显示，或完整写入路径明确的日志文件。
- 写入日志时说明路径，并在命令完成后检查和报告关键结果。
- 不得丢弃输出、用静默选项隐藏过程和错误，或捕获输出后既不显示也不保存。命令本身没有输出不属于静默丢弃。
- 实验命令还须遵守下方的实验记录规范。

## 文档管理

### 存储分类

长期文档统一存入 `docs/`：

- `docs/arch/`：架构、模块职责、接口、数据流和部署结构。
- `docs/missions/`：任务说明、实施方案、执行指导、检查清单和验收标准。
- `docs/guides/`：环境搭建、开发、使用、测试、部署和运维指南。
- `docs/reports/`：调研、复盘、评审和阶段总结。
- `docs/references/`：协议说明、外部规范摘要和其他参考资料。
- `docs/specs/`：已批准或待执行的任务规格。
- `docs/adr/`：架构决策记录。

工具链或社区惯例要求固定位置的 `README.md`、`AGENTS.md` 和许可证文件可以留在根目录。`issues/` 保存任务交工单、审查和验证等历史证据，不改写其事实内容。

新文档的文件名应直接描述内容，建议使用小写 kebab-case，不加时间前缀。通过元数据和版本记录维护历史，不复制带日期的版本文件。出现新的文档类型时，按用途创建清晰的分类目录，不得混放。

架构文档必须与当前代码一致。影响架构、模块职责、接口、数据流或部署方式的代码变更，须在同一任务中检查并更新相关文档。尚未实现的设计明确标为“规划”或“提案”，当前实现必须可以核验。

### 元数据与版本记录

创建或实质性修改 Markdown 文档时，在一级标题正下方保留：

```markdown
# 文档标题

> 最后修改时间：YYYY-MM-DD HH:mm TZ
> 文档版本：v1
> 修改说明：简要说明本次新增、修订或删除的内容。
```

时间使用当前环境时区。新文档从 `v1` 开始；每次实质性修改时，更新时间、说明和版本，版本严格递增一次。只读检查不更新元数据；已有文档可在下次实质性修改时采用此格式，不得仅为补充元数据而修改。

每份文档末尾须保留完整版本修改记录，新增版本只追加，不改写或删除历史；最新记录须与顶部元数据一致。新建或实质性修改文档后，交付前使用匹配的专项 skill 润色并完成其前置流程；没有匹配 skill 时使用通用语言润色。删除重复、空泛、歧义和机械化表达，但不得改变事实、数据、引用、实验结果、结论强度、不确定性或必要的可追溯信息。

## 实验记录

实验的配置、日志、中间数据、原始结果和分析统一保存在 `records/`。每次启动实验前创建新的独立子目录，不得共用或覆盖运行目录。目录名应唯一且便于辨认，建议使用 `YYYYMMDD-HHMMSS-实验名称`。

每个实验目录必须包含 `config.md` 和 `analysis.md`：前者记录启动时间、目的、命令、代码版本、运行环境、输入数据和参数，并及时补充变更、异常、重启和人工干预；后者只分析本目录内本次产生的数据，并注明原始文件、日志位置或统计命令。

严禁编造、篡改数据，将估算冒充实测，或将其他实验数据冒充本次结果。未产生、缺失或无法确认的数据，明确记录状态和原因，不得用推测值填补。后台实验使用持久会话或任务管理工具，并保存启动命令、日志和运行状态。批量实验启动后先观察前几个参数点，确认进程持续运行、结果文件非空且日志无致命错误，再停止人工监控；启动阶段失败时先停止并排查。

## 版本修改记录

| 版本 | 修改时间 | 修改内容 |
| --- | --- | --- |
| v1 | 2026-09-24 10:27 CST | 抽离通用协作规范，移除项目名称、专属构建配置和环境参数。 |
| v2 | 2026-09-24 04:37 CEST | 按执行顺序重排规范，合并重复条款并明确文档与历史证据的存储边界。 |
| v3 | 2026-09-24 18:16 CST | 增加文件格式必须遵守 `.editorconfig` 和 `.clang-format` 的约束。 |
